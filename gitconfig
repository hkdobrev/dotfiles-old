[user]
	name = Haralan Dobrev
	email = hkdobrev@gmail.com
[gui]
	recentrepo = /home/hkdobrev/Projects/vagrant/flingflung
[core]
	editor = vim
[alias]
	a = add
	aa = add --all
	ae = add --edit
	ai = add --interactive
	amend = commit --amend -C HEAD
	b = branch
	bd = branch -d
	c = commit
	ch = checkout
	cleanit = clean -fd
	cn = config
	cng = config --global
	d = difftool -y --tool=vimdiff
	dc = diff --cached
	div = divergence
	ds = diff --staged
	f = fetch
	h = help
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	m = merge
	nm = branch --no-merged
	nmm = branch --no-merged master
	p = push
	pl = pull
	r = remote
	ru = remote update
	ruo = remote update origin
	rb = rebase
	rs = reset
	s = status -sb
	sh = show
	shl = shortlog
	st = stash
	t = tag
[diff]
	renames = copies
[pager]
	color = true
[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red
	new = green
[color "status"]
	added = green
	changed = yellow
	untracked = cyan
[branch]
	autosetuprebase = always
[push]
	default = tracking
[github]
	user = hkdobrev
[gc]
	auto=1
[merge]
	summary=true
